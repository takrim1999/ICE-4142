USE pubs;
-- SELECT DISTINCT type FROM titles; 
-- BETWEEN AND
-- LIKE %,_ -- here % is for all char and _ for one char like: LIKE '%gmail%' will return all mail with gmail.com
-- IS NULL
-- WHERE DISTINCT type
-- ORDER BY title , name -- this will first order by title then for same title by name column